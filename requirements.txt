# Smart Campus AI Chatbot v3 — requirements
# Install: pip install -r requirements.txt
Flask==3.1.0
gunicorn==23.0.0
google-generativeai
python-dotenv
requests
flask-compress==1.14
scikit-learn==1.5.0
numpy==1.26.4
nltk==3.8.1
spacy==3.7.2

# Response compression — reduces payload for mobile networks
flask-compress==1.14

# NLP — scikit-learn TF-IDF + Naive-Bayes (updated for Python 3.12)
scikit-learn==1.5.0
numpy==1.26.4

# NLTK — stopwords, stemming, tokenisation
nltk==3.8.1

# spaCy — lemmatisation, NER (en_core_web_sm model needed)
# After install run: python -m spacy download en_core_web_sm
spacy==3.7.2

# Google Translate — multilingual support
googletrans==4.0.0rc1

# (Optional) bcrypt — alternative password hashing
# bcrypt==4.0.1
